{
  "name": "@labelloop/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The gateway and platform API: public /v1 REST plus the internal console surface.",
  "scripts": {
    "dev": "bun --env-file=../../.env --hot src/server.ts",
    "start": "bun --env-file=../../.env src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun --env-file=../../.env test"
  },
  "dependencies": {
    "@hono/zod-openapi": "^1.6.1",
    "@labelloop/contracts": "workspace:*",
    "@labelloop/db": "workspace:*",
    "@scalar/hono-api-reference": "^0.11.16",
    "@sentry/bun": "^10.70.0",
    "better-auth": "^1.7.1",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.13.3",
    "hono-pino": "^0.10.3",
    "pg-boss": "^12.28.0",
    "pino": "^10.3.1",
    "zod": "^4.4.3"
  }
}
